stages:
    - build
    - test
    - staging

variables:
    CONTAINER_IMAGE: "${CI_REGISTRY_IMAGE}:${CI_COMMIT_REF_NAME}"
    POSTGRES_DB: tedetis
    POSTGRES_USER: tedetis
    POSTGRES_PASSWORD: tedetis

build:
  stage: build
  tags:
    - docker-build
  script:
    - /bin/bash docker-resources/generate-manifest.sh > manifest.txt
    - docker login -u "gitlab-ci-token" -p "$CI_JOB_TOKEN" $CI_REGISTRY
    - docker build --pull -t ${CONTAINER_IMAGE} .
    - docker push ${CONTAINER_IMAGE}

unit_test:
    stage: test
    image : "$CONTAINER_IMAGE"
    services:
        - postgres:latest
    variables:
        XDEBUG_ON: "true"
        POSTGRES_USER_TEST: tedetis
        POSTGRES_PASSWORD_TEST: tedetis
        POSTGRES_DB_TEST: tedetis
        POSTGRES_HOST_TEST: postgres
        POSTGRES_HOST: postgres
        S2LOW_WEBSITE: "http://localhost/"
        S2LOW_WEBSITE_SSL: "https://localhost/"
    script:
        - /usr/local/lib/composer/vendor/bin/phpunit --coverage-text --colors=never -c /var/www/s2low/test/PHPUnit/phpunit.xml

deploy_app:
    stage: staging
    only:
        - master
    image: kroniak/ssh-client
    script:
        - eval $(ssh-agent -s)
        - ssh-add <(echo "$SSH_PRIVATE_KEY")
        - mkdir -p ~/.ssh
        - '[[ -f /.dockerenv ]] && echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config'
        - scp ./docker-compose.yml s2low@s2low.test.libriciel.fr:/data/s2low/docker-file
        - scp ./docker-compose.staging.yml s2low@s2low.test.libriciel.fr:/data/s2low/docker-file/docker-compose.override.yml
        - echo "$STAGING_ENV_FILE_CONTENT" > /tmp/.env
        - scp /tmp/.env s2low@s2low.test.libriciel.fr:/data/s2low/docker-file/
        - scp ./docker-resources/Dockerfile* ./docker-resources/docker_postgres.sh s2low@s2low.test.libriciel.fr:/data/s2low/docker-file/docker-resources
        - ssh -tt s2low@s2low.test.libriciel.fr "docker login -u gitlab-ci-token -p '$CI_JOB_TOKEN' $CI_REGISTRY && cd /data/s2low/docker-file/  && docker-compose pull && docker-compose up -d"
