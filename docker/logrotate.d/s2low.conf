/data/log/*.log {
    daily
    missingok
    rotate 14
    compress
    delaycompress
    notifempty
    postrotate
        /bin/kill -s SIGUSR2 $(cat /data/run/supervisord.pid) 2>/dev/null || true
    endscript
}
